import 'package:flutter/material.dart';

class AdminManageMembersPage extends StatelessWidget {
  const AdminManageMembersPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
